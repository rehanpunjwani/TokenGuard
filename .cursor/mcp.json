{
  "mcpServers": {
    "recall-py": {
      "command": "bash",
      "args": ["scripts/mcp-stdio.sh"],
      "cwd": "${workspaceFolder}",
      "env": {
        "RECALL_PY_WORKSPACE": "${workspaceFolder}"
      }
    }
  }
}
